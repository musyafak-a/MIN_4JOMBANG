from django.contrib import admin
from .models import (
    SchoolProfile, CarouselSlide, Achievement,
    OverviewStat, GalleryPhoto, Partnership, Berita, Alumni
)

# Models are now managed via custom Admin Sekolah Panel
